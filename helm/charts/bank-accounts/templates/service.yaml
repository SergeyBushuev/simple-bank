apiVersion: v1
kind: Service
metadata:
  name: {{ include "bank-accounts.fullname" . }}
  labels:
    {{- include "bank-accounts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bank-accounts.selectorLabels" . | nindent 4 }}
