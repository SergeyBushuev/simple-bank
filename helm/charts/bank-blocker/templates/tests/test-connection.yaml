apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bank-blocker.fullname" . }}-test-connection"
  labels:
    {{- include "bank-blocker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bank-blocker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
