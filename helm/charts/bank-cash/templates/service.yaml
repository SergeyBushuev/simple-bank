apiVersion: v1
kind: Service
metadata:
  name: {{ include "bank-cash.fullname" . }}
  labels:
    {{- include "bank-cash.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bank-cash.selectorLabels" . | nindent 4 }}
