apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bank-cash.fullname" . }}-test-connection"
  labels:
    {{- include "bank-cash.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bank-cash.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
