apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bank-exchange-generator.fullname" . }}-test-connection"
  labels:
    {{- include "bank-exchange-generator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bank-exchange-generator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
