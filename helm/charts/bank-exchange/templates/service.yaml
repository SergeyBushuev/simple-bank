apiVersion: v1
kind: Service
metadata:
  name: {{ include "bank-exchange.fullname" . }}
  labels:
    {{- include "bank-exchange.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bank-exchange.selectorLabels" . | nindent 4 }}
