apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bank-exchange.fullname" . }}-test-connection"
  labels:
    {{- include "bank-exchange.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bank-exchange.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
