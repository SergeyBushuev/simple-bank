apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bank-gateway.fullname" . }}-test-connection"
  labels:
    {{- include "bank-gateway.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bank-gateway.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
