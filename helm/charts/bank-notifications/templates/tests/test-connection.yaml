apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bank-notifications.fullname" . }}-test-connection"
  labels:
    {{- include "bank-notifications.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bank-notifications.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
