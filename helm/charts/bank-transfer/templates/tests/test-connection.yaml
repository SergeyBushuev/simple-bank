apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bank-transfer.fullname" . }}-test-connection"
  labels:
    {{- include "bank-transfer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bank-transfer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
